<!-- views/pages/details.ejs -->

<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= restaurant ? restaurant.name : '맛집 정보' %> | Restaurant Search</title>
  <link rel="stylesheet" href="./stylesheets/styles.css">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="details-container">
    <% if (restaurant) { %>
      <section class="details-info-section">
        <div class="details-basic-info">
          <h1 class="details-name"><%= restaurant.name %></h1>
          <img src="<%= restaurant.imageUrl %>" alt="<%= restaurant.name %>" class="details-image">
          <p class="details-address">주소: <%= restaurant.address %></p>
          <p class="details-phone">전화번호: <%= restaurant.phone %></p>
          <p class="details-hours">영업시간: <%= restaurant.hours %></p>
          <p class="details-rating">평점: <%= restaurant.rating %></p>
        </div>

        <!-- 즐겨찾기 버튼 (로그인 상태일 때만 표시) -->
        <% if (user) { %>
          <form action="/favorites" method="post" class="details-favorite-form">
            <input type="hidden" name="restaurantId" value="<%= restaurant.id %>">
            <% if (isFavorited) { %>
              <button type="submit" name="action" value="remove" class="details-favorite-button">즐겨
