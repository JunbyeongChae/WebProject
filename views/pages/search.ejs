<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>검색 결과 | Restaurant Search</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="search-container">
    <!-- 검색 섹션 -->
    <section class="search-form-section">
      <form action="/search" method="get" class="search-form">
        <input type="text" name="query" placeholder="맛집 키워드를 입력하세요" class="search-input" value="<%= query || '' %>" required>
        
        <select name="region" class="search-select">
          <option value="">지역선택</option>
          <option value="구로" <%= (region === '구로') ? 'selected' : '' %>>구로</option>
          <option value="금천" <%= (region === '금천') ? 'selected' : '' %>>금천</option>
          <option value="광명" <%= (region === '광명') ? 'selected' : '' %>>광명</option>
        </select>
        
        <button type="submit" class="search-button">검색</button>
      </form>
    </section>

    <!-- 지도 섹션 -->
    <section class="search-map-section">
      <div id="map" class="search-map"></div>
    </section>

    <!-- 검색 결과 섹션 -->
    <section class="search-results-section">
      <h2 class="search-results-title">검색 결과</h2>